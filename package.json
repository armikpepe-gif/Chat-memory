{
  "name": "chat-memory",
  "version": "1.0.0",
  "description": "Chat memory project on Render",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "npm install --legacy-peer-deps || true"
  },
  "dependencies": {
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=16.x"
  }
}
